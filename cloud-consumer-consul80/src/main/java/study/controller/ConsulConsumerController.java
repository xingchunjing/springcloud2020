package study.controller;import org.springframework.beans.factory.annotation.Value;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.RestController;import org.springframework.web.client.RestTemplate;import javax.annotation.Resource;@RestControllerpublic class ConsulConsumerController {    @Value("${server.port}")    private String serverPort;    public static final String INVOKE_URL = "http://cloud-provider-gesoten";    @Resource    private RestTemplate restTemplate;    @GetMapping("/consumer/test/co")    public String consumerCo(){        String result = restTemplate.getForObject(INVOKE_URL + "/test/co",String.class);        return result;    }}